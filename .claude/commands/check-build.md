Run the full build pipeline to verify everything compiles, tests pass, and lint is clean.

Steps:
1. Run `make build` — verify binary compiles
2. Run `make test` — verify all tests pass
3. Run `make lint` — verify no lint issues

If any step fails, diagnose and fix the issue. Report results concisely.
